type Query {
  refreshToken: String!
}

type Mutation {
  login(user: LoginUserInput!): LoginResult!
}

type LoginResult {
  user: User!
  token: String!
}

input LoginUserInput {
  username: String
  email: String
  password: String!
}
